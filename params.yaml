# DVC参数配置文件
data:
  path: "C:/Users/10385/Desktop/echo/tests/test_data/Corel-1000"
  test_size: 0.2
  random_state: 42

preprocess:
  image_size: [128, 128]
  grayscale: true
  normalize: true

pca:
  n_components: 50
  random_state: 42

retrieval:
  top_k: 10
  distance_metric: "euclidean"

evaluation:
  n_queries: 50
  random_state: 42

mlflow:
  tracking_uri: "http://localhost:5001"
  experiment_name: "PCA_Image_Retrieval"