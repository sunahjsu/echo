
# 基础Web框架
flask==2.2.5
gunicorn==21.2.0  # Linux生产环境用
waitress==2.1.2   # Windows生产环境用（二选一）

# 数据处理与机器学习
opencv-python==4.5.5.64
scikit-learn==1.2.2
numpy>=1.22.0,<2.0  # 限制numpy在1.x范围，兼容mlflow和opencv
matplotlib==3.5.3
pandas==1.3.5
scipy>=1.8.0
protobuf<5.0        # 新增：限制protobuf版本，兼容mlflow

# MLOps工具
mlflow==2.8.0
dvc==2.58.0
dvc-s3>=3.0.0  # 替换为实际使用的远程存储类型（如dvc-azure）

# 开发与CI工具
python-dotenv==0.19.2  # 环境变量管理
black==23.12.1  # 代码格式化
flake8==6.0.0   # 语法检查
packaging==23.1 # 版本处理依赖

dagshub>=0.3.10  # 较新稳定版，兼容性更好